extends Node

const FILES_PATH = "user://files"
const MANIFEST_NAME = "manifest.json"
const INDEX_NAME = "index.json"

func add_files_to_sessions(file_paths: PackedStringArray, campaign_ids: PackedStringArray = []) -> Error:
	
	return OK
